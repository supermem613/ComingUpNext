namespace ComingUpNextTray
{
    // A placeholder/host form for potential future UI expansion. Currently the tray app
    // operates without showing a window, but keeping a named form clarifies intent and
    // avoids the default Form1 naming.

    /// <summary>
    /// Represents the tray host form for the application.
    /// </summary>
    internal partial class TrayHostForm : Form
    {
        /// <summary>
        /// Initializes a new instance of the <see cref="TrayHostForm"/> class.
        /// </summary>
        public TrayHostForm()
        {
            this.InitializeComponent();
        }
    }
}
