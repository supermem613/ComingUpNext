namespace ComingUpNextTray
{
    /// <summary>
    /// Centralized UI layout numeric constants used across the tray UI.
    /// </summary>
    internal static class UiLayout
    {
        /// <summary>Default width in pixels for the hover window.</summary>
        public const int DefaultHoverWindowWidth = 420;

        /// <summary>Default maximum text width in pixels used for truncation (menu/hover title).</summary>
        public const int DefaultMaxTextWidth = 420;

        /// <summary>Default height in pixels for the hover window.</summary>
        public const int DefaultHoverWindowHeight = 60;
    }
}
